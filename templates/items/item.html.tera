{% extends "base" %}

{% block title %}{{ item.name }}{% endblock %}

{% block content %}
    <h1>{{ item.name }}</h1>
    
    <p>Опис: <br>{{ item.description }}</p>
    <p>Ціна: {{ item.price }}</p>
    <p>Категорія: {{ item.category }}</p>
{% endblock %}